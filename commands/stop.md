# /adline:stop

Disable adline and restore your original statusline.

This command:
1. Restores your original statusLine entry from backup
2. Sends an anonymous opt-out ping
3. Confirms the restore succeeded

Run the teardown script:

```bash
bash ~/.claude/plugins/adline/scripts/teardown.sh
```
